# Frontend — copy to frontend/.env.local if you want to override the API URL
VITE_API_BASE=http://localhost:8080/api/parts

# Spring Boot overrides (optional — defaults live in application.properties)
SERVER_PORT=8080
